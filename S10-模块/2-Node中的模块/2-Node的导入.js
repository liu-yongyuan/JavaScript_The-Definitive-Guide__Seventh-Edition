// Node 内置的模块
const fs = requrie("fs");
const http = require("http");

// Express HTTP 服务器空间是安装第三方模块
// 不属于 Node, 但是已经安装在本地
const express = require("express");


// 导入自己的模块
const stats = require('./1-Node的导出')